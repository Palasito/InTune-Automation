Use the script with the parameters: -JSONPath, -Prefix

JSONPath is used to define the folder in which are all the jsons that we want to import

Prefix is used to change policy's append short texts before policy's DisplayName