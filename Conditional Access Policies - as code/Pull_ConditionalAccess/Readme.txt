This script is used to export all conditional access policies in the tenant. Output format is "DisplayName.json"

It can used with the following custom parameters: -ExportPath

ExportPath is pretty self explanatory, you define the folder in which all policy jsons are to be created.